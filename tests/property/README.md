# Property Tests

Property tests exercise reversible E2M1 packing and CUTLASS 128x4 scale-layout
mappings over generated logical shapes and byte payloads. Golden values remain
hand-authored fixtures under `tests/fixtures`; property tests do not replace
independent examples.
